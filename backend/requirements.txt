# FastAPI 和 Web 框架
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-multipart==0.0.26
websockets==16.0

# 数据库和ORM
sqlalchemy==2.0.49
alembic==1.18.4
aiosqlite==0.22.1

# 数据处理和存储
# Python 3.13 兼容数据栈：pyarrow 14 无 cp313 wheel，且与 NumPy 2 不兼容
pandas==2.3.3
numpy==2.4.4
orjson>=3.9.0  # 高性能 JSON（回测报告/缓存哈希，见 docs/backtest-optimization.md）
pyarrow==23.0.1
polars==0.19.19
scipy==1.17.1  # 科学计算库，用于统计分析和风险计算
openpyxl>=3.1.0  # Excel文件处理
reportlab>=4.0.0  # PDF报告生成

# 量化金融和机器学习
# Qlib需要从GitHub安装（Python 3.13兼容）:
# pip install git+https://github.com/microsoft/qlib.git
# 注意：这会安装pyqlib包（Microsoft Qlib量化投资平台）
# pyqlib>=0.9.8.dev18  # Microsoft Qlib量化投资平台，从GitHub安装（需要单独安装）
git+https://github.com/microsoft/qlib.git  # Microsoft Qlib量化投资平台
vectorbt>=0.26.0
# ta-lib==0.4.28  # 暂时注释掉，因为编译问题
# pandas-ta>=0.4.67b0  # 已移除，使用内置的pandas/numpy实现替代
scikit-learn>=1.4.0
joblib>=1.3.0  # scikit-learn的依赖，用于模型序列化
optuna>=3.4.0  # 超参数优化框架
# PyTorch - 支持ROCm和CUDA
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0
transformers==4.35.2
xgboost==2.0.2

# HTTP 客户端
httpx==0.28.1
aiohttp==3.13.5

# 异步事件循环支持
nest-asyncio>=1.6.0  # 允许在已有事件循环中嵌套运行异步代码

# SFTP 客户端
paramiko==4.0.0

# 配置和环境
pydantic==2.13.3
pydantic-settings==2.14.0
python-dotenv==1.0.0

# 测试框架
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-mock==3.15.1
hypothesis==6.152.2
pytest-cov==7.0.0

# 开发工具
black==26.3.1
isort==8.0.1
flake8==7.3.0
mypy==1.20.2
bandit==1.9.2  # 安全扫描
# safety intentionally not pinned here: install it separately in CI/security jobs
radon==6.0.1  # 代码复杂度分析（可选）

# 日志和监控
loguru==0.7.3
prometheus-client==0.23.1
psutil==7.1.4
GPUtil==1.4.0  # GPU监控库

# 图论和数据分析
networkx>=3.0  # 用于数据血缘追踪