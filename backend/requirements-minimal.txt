# 最小化依赖 - 用于快速启动和测试
# FastAPI 和 Web 框架
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
websockets>=12.0

# 数据库和ORM
sqlalchemy>=2.0.25
alembic>=1.13.0
aiosqlite>=0.20.0
greenlet>=3.0.0  # SQLAlchemy异步操作必需

# 基础数据处理
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.11.0  # 科学计算库，用于统计分析
pyarrow>=14.0.0  # Parquet文件支持，用于数据同步和存储
fastparquet>=2023.10.1  # Parquet文件备选引擎，用于处理旧版本文件

# 机器学习
scikit-learn>=1.4.0  # 机器学习库，用于特征选择和模型训练
torch>=2.1.0  # PyTorch深度学习框架
torchvision>=0.16.0  # PyTorch视觉库
xgboost>=2.0.0  # XGBoost梯度提升树，用于机器学习
# pandas-ta>=0.4.67b0  # 技术分析库，暂时注释掉（不支持Python 3.14）

# HTTP 客户端
httpx>=0.27.0
requests>=2.31.0  # 基础HTTP请求库，用于Qlib等功能

# 异步事件循环支持
nest-asyncio>=1.6.0  # 允许在已有事件循环中嵌套运行异步代码（回测任务需要）

# SFTP 客户端
paramiko>=3.4.0

# 配置和环境
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv==1.0.0

# 日志和监控
loguru>=0.7.2
prometheus-client>=0.20.0