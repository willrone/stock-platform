# 后端质量门禁依赖集合
#
# 目的：给未来 GitHub Actions 质量门提供稳定、轻量、可复现的安装入口。
# 当前 workflow 仍使用 requirements.txt（token 暂无 workflow scope，暂不能切换）。
# 版本策略必须和当前已绿 CI 保持一致，尤其是 black/isort/flake8。

# backend/app 基础导入所需运行时依赖
# Python 3.13 兼容运行时栈：旧 pin 多数无 cp313 wheel 或依赖 Python 3.12 API
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-multipart==0.0.26
websockets==16.0
sqlalchemy==2.0.49
alembic==1.18.4
aiosqlite==0.22.1
pandas==2.3.3
numpy==2.4.4
scipy==1.17.1
pyarrow==23.0.1
httpx==0.28.1
aiohttp==3.13.5
pydantic==2.13.3
pydantic-settings==2.14.0
python-dotenv==1.0.0
loguru==0.7.3
prometheus-client==0.23.1
joblib>=1.3.0
scikit-learn>=1.4.0
paramiko==4.0.0

# 测试/收集依赖（供 quality smoke 与 import checks 使用）
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-mock==3.15.1
hypothesis==6.152.2
pytest-cov==7.0.0

# 静态检查工具：必须与当前 GitHub 已绿门禁一致
black==26.3.1
isort==8.0.1
flake8==7.3.0
mypy==1.20.2
bandit==1.9.2
radon==6.0.1
